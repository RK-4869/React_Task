<!-- File: README.md -->

地域コミュニティの活性化を目的としたイベント管理アプリ「まちイベ」のフロントエンド実装リポジトリになっております。

# アプリ名「まちイベ」

## 概要

地域住民が気軽にイベント情報を閲覧・作成・参加できるWebアプリケーションです。

近年、都市部への人口集中やライフスタイルの多様化、さらには新型コロナウイルス感染症の影響から地域住民同士のつながりが薄くなってきていることを課題として感じていました。
その一方で、地域住民からは「地域のイベントを気軽に知りたい」や「様々な世代が交流できる場がほしい」などのお声も聞かれました。
今回ご提案させていただくイベント「街イベ」には、住民同士の交流の促進や経済の活性化にもつながっていく可能性を秘めていると願っています。

---

## クイックスタート(利用者向け)

### 前提条件

- Node.js v18以上
- npm v9以上

### インストール・起動

```bash
git clone https://github.com/RK-4869/React_Task.git
cd React_Task/community-event-app
npm install
npm start
```

ブラウザで http://localhost:3000 を開いてください。

---

## セットアップ手順

### React開発環境のセットアップ手順

①次にReactを用いての実装は初めてだったので、React開発環境のセットアップ手順を実装しました。
最初に、Node.js公式サイトにアクセスし、Node.jsをダウンロードしインストール。
②Git Bashでnode -vコマンドとnpm -vコマンドをそれぞれ入力しバージョン番号が表示されるかを確認。
③Reactプロジェクトの作成をしました。今回のプロジェクトを置きたいフォルダ「React_Task」に移動。
その後、Git Bash上でnpx create-react-app community-event-appコマンドを入力。
④OK to proceed?と表示されたら、yを入力しEnter。Happy hacking!が表示されれば完了。

### 起動確認

①実際にプロジェクトフォルダに移動する。
まず、cd community-event-appを入力し、その後npm startと打つ。
Compiled successfully!
Local: http://localhost:3000　
上記のような表示がされていれば正常な状態。ターミナルはそのまま閉じない。

### 新しいターミナルを開く

①npm startを実行したターミナルはそのまま使用するので、新しくターミナルを開く。
ターミナルを開く際は、ターミナル右上の+ボタンを押下する。

### react-router-domのインストール

①新規のターミナルで、npm install react-router-domコマンドを実行する。

### フォルダ構造の作成

①mkdir src/componentsコマンドを入力。
②mkdir src/pagesコマンドを入力。
③mkdir src/dataコマンドを入力。
④実際にVSCode上の左側のウィンドウscr内に①~③が作成されていればOK。

### GitHubとの連携

①GitHubの右上にある+ボタンからNew Repositoryを選択する。
Repository name:React_Task
Initialize this repository with a READMEはチェックしない。
② ①の設定まで確認出来たらCreate Repositoryで、リポジトリを作成する。

### VSCode上から作成したリポジトリにpush

①git remote add origin https://github.com/RK-4869/React_Task と入力。
②git branch -M main
③git push -u origin main
すべてのコマンドを順番に入力する。
ここでもし、remote contains work that you do not have locallyとエラーが表示された場合のみ
git push -u origin main --forceコマンドを入力し数秒待機。
④エラー文がなければOK。

### 最初のコミット

①git add . コマンドを入力。
②git commit -m コマンドでコミットメッセージを入力。
③git push

### 連携確認

①実際にGitHubアカウント内のReact_Taskリポジトリを確認。
②入力したコミットメッセージが反映されていることを確認。
③連携確認完了。

### セットアップ完了チェックリスト

| #   | 確認項目                                                          | 状態 |
| --- | ----------------------------------------------------------------- | ---- |
| 1   | `node -v` でバージョンが表示される                                | ✅   |
| 2   | `npm -v` でバージョンが表示される                                 | ✅   |
| 3   | `npm start` でブラウザにReact画面が表示される                     | ✅   |
| 4   | VSCodeでプロジェクトフォルダが開けている                          | ✅   |
| 5   | `react-router-dom` が `package.json` の dependencies に入っている | ✅   |
| 6   | `src/components`、`src/pages`、`src/data` フォルダがある          | ✅   |
| 7   | GitHubの `React_Task` にコードがpushされている                    | ✅   |

## 使用技術・ライブラリ

| 技術             | バージョン | 用途                             |
| ---------------- | ---------- | -------------------------------- |
| React            | 18.x       | UIライブラリ                     |
| react-router-dom | 6.x        | ページルーティング（SPA遷移）    |
| CSS Modules      | —          | コンポーネント単位のスタイリング |
| Create React App | 5.x        | プロジェクト生成・ビルド環境     |

> バックエンドは使用せず、モックデータ（`src/data/events.json`）でイベント情報を管理しています。

---

## 実装機能

### 1. イベント一覧ページ（トップページ）

- モックデータからイベントをカード形式で一覧表示
- **キーワード検索**：タイトル・説明文を部分一致でリアルタイムにフィルタ
- **カテゴリフィルタ**：「すべて / お祭り / ワークショップ / スポーツ / その他」のボタンで絞り込み
- 検索結果が0件の場合、案内メッセージを表示
- カードクリックで詳細ページへ遷移

### 2. イベント詳細ページ

- イベントの全情報を表示（タイトル・日時・場所・主催者・説明）
- カテゴリごとに色分けしたバッジで視認性を向上
- **「参加する」ボタン**：クリックで参加者数が+1、ボタンが「参加済み」に変化（二重参加防止）
- 「← イベント一覧に戻る」リンクで一覧へ遷移

### 3. イベント作成ページ

- フォーム入力（イベント名・日付・時間・場所・カテゴリ・主催者・説明）
- **バリデーション**：必須項目が未入力の場合、各項目ごとにエラーメッセージを赤字で表示
- 作成完了後、「🎉 イベントを作成しました！」メッセージを表示し、2秒後に一覧へ自動遷移
- 作成したイベントが一覧ページに即時反映

### 4. 共通機能

- **レスポンシブ対応**：PC・タブレット・スマートフォンで快適に閲覧・操作可能
- **ヘッダーナビゲーション**：「イベント一覧」「+ イベントを作成」への導線を常に表示
- **ステート管理**：`App.js` でイベントデータを `useState` で一元管理し、各ページに `props` で受け渡し

---

## 動作確認方法

| #   | 操作                                             | 期待される結果                                                         |
| --- | ------------------------------------------------ | ---------------------------------------------------------------------- |
| 1   | トップページ（`/`）を開く                        | イベントカードが6件表示される                                          |
| 2   | 検索バーに「ヨガ」と入力                         | 「朝ヨガ in 河川敷」のカードのみ表示される                             |
| 3   | カテゴリ「お祭り」をクリック                     | お祭りカテゴリのカードのみ表示される                                   |
| 4   | 存在しないキーワード（例：「あいうえお」）で検索 | 「該当するイベントが見つかりませんでした」が表示される                 |
| 5   | イベントカードをクリック                         | 詳細ページに遷移し、全情報が表示される                                 |
| 6   | 「🙋 参加する」ボタンをクリック                  | 参加者数が+1され、ボタンが「✅ 参加済み」に変化する                    |
| 7   | 「← イベント一覧に戻る」をクリック               | 一覧ページに戻る                                                       |
| 8   | ヘッダーの「+ イベントを作成」をクリック         | 作成フォームが表示される                                               |
| 9   | 何も入力せずに送信ボタンを押す                   | 各項目にバリデーションエラーが赤字で表示される                         |
| 10  | 全項目を入力して送信                             | 完了メッセージ表示 → 2秒後に一覧に戻り、新しいイベントが追加されている |

---

## フォルダ構成

```
src/
├── components/          # 再利用可能なUIコンポーネント
│   ├── Header.jsx / Header.module.css
│   ├── SearchBar.jsx / SearchBar.module.css
│   ├── CategoryFilter.jsx / CategoryFilter.module.css
│   └── EventCard.jsx / EventCard.module.css
├── pages/               # 各ページコンポーネント
│   ├── EventListPage.jsx / EventListPage.module.css
│   ├── EventDetailPage.jsx / EventDetailPage.module.css
│   └── EventCreatePage.jsx / EventCreatePage.module.css
├── data/                # モックデータ
│   └── events.json
├── App.js               # ルーティング・ステート管理
├── index.js             # エントリーポイント
└── index.css            # グローバルスタイル
```

---

## 顧客視点の工夫

| 観点                 | 具体的な工夫                                                         |
| -------------------- | -------------------------------------------------------------------- |
| **見やすさ**         | フォントサイズ16px以上、十分なコントラスト比で年配の方にも配慮       |
| **迷わない操作**     | ボタン・リンクにアクション名を明記（「参加する」「一覧に戻る」等）   |
| **情報の優先順位**   | カードにはタイトル → 日時 → 場所の順で重要情報を上から配置           |
| **カテゴリの視認性** | カテゴリごとに色分けしたバッジで一目で判別可能                       |
| **空状態への配慮**   | 検索結果0件時に案内メッセージを表示し、次の行動を促す                |
| **モバイル対応**     | レスポンシブデザインでスマートフォンからも快適に利用可能             |
| **入力ミスの防止**   | フォームに必須項目のバリデーションとプレースホルダー（入力例）を設置 |

---

## 開発を通じての所感

本アプリケーションは、未経験エンジニアがReactを使ったフロントエンド開発に初めて取り組んだ成果物になっております。
